<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xushu.bookmanage.mapper.BookMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into book (title, author, isbn, category_id, publisher, publish_date, price, stock, description, cover, create_time, update_time)
        values (#{title}, #{author}, #{isbn}, #{categoryId}, #{publisher}, #{publishDate}, #{price}, #{stock}, #{description}, #{cover}, now(), now())
    </insert>

    <select id="countByIsbn" resultType="int">
        select count(*) from book where isbn = #{isbn}
    </select>

    <select id="selectById" resultType="com.xushu.bookmanage.entity.Book">
        select * from book where id = #{id}
    </select>

</mapper>
